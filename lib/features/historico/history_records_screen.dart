import 'package:flutter/material.dart';

class HistoryRecordsScreen extends StatelessWidget {
  const HistoryRecordsScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) => Scaffold(
      appBar: AppBar(title: const Text('Registros')),
      body: const Center(child: Text('Registros')));
}
